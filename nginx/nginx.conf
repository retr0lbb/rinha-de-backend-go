events {
    worker_connections 1024;
}

http {
    upstream backend {
        server api1:9999;
        server api2:9999;
        keepalive 32; # reutiliza conexões com as apis
    }

    server {
        listen 9999;

        location / {
            proxy_pass http://backend;
            proxy_http_version 1.1;
            proxy_set_header Connection ""; # necessário para keepalive funcionar
        }
    }
}