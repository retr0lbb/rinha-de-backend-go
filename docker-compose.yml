services:
  api1:
    image: retr0lbb/rinha-backend-2026:v7
    platform: linux/amd64
    container_name: api1
    hostname: api1
    mem_limit: 140m
    cpus: "0.45"

  api2:
    image: retr0lbb/rinha-backend-2026:v7
    platform: linux/amd64
    container_name: api2
    hostname: api2
    mem_limit: 140m
    cpus: "0.45"

  nginx:
    image: nginx:alpine
    platform: linux/amd64
    container_name: nginx
    ports:
      - "9999:9999"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api1
      - api2
    mem_limit: 50m
    cpus: "0.10"
